export class Skill {

    // name de type string
    // logo de type string
    // site de type string

    constructor(
        public name: string,
        public logo: string,
        public site: string) {
    }
}